import { useRef } from "react";
type Props={image?:string|null;title:string;price?:string|null;color?:string;downloadLabel:string;tagline:string;};
export default function CreativePreview({image,title,price,color="#5B3DF5",downloadLabel,tagline}:Props){const canvasRef=useRef<HTMLCanvasElement>(null);const download=async()=>{const canvas=canvasRef.current;if(!canvas)return;const ctx=canvas.getContext("2d");if(!ctx)return;canvas.width=1080;canvas.height=1080;const gradient=ctx.createLinearGradient(0,0,1080,1080);gradient.addColorStop(0,color);gradient.addColorStop(1,"#20104f");ctx.fillStyle=gradient;ctx.fillRect(0,0,1080,1080);if(image){try{const img=new Image();img.crossOrigin="anonymous";img.src=image;await new Promise((resolve,reject)=>{img.onload=resolve;img.onerror=reject;});const maxW=820,maxH=650;const ratio=Math.min(maxW/img.width,maxH/img.height);const w=img.width*ratio,h=img.height*ratio;ctx.fillStyle="white";ctx.roundRect(100,90,880,700,42);ctx.fill();ctx.drawImage(img,(1080-w)/2,120+(650-h)/2,w,h);}catch{}}ctx.fillStyle="white";ctx.font="700 64px Arial";ctx.fillText(title.slice(0,28),90,870);if(price){ctx.font="800 72px Arial";ctx.fillText(price,90,970);}ctx.font="500 30px Arial";ctx.fillText(tagline,620,1010);const a=document.createElement("a");a.download=`${title.replace(/[^a-z0-9]/gi,"-").toLowerCase()}-launchpilot.png`;a.href=canvas.toDataURL("image/png");a.click();};return <div className="lp-creative-preview"><div className="lp-creative-card" style={{background:`linear-gradient(135deg, ${color}, #20104f)`}}>{image?<img src={image} alt={title}/>:<div className="lp-image-placeholder">📦</div>}<div className="lp-creative-copy"><strong>{title}</strong>{price&&<span>{price}</span>}</div></div><canvas ref={canvasRef} style={{display:"none"}}/><button className="lp-native-button" type="button" onClick={download}>{downloadLabel}</button></div>;}
